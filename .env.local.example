# 로닛 (loneat.kr) 환경 변수 설정
# 
# 이 파일을 .env.local로 복사한 후 값을 입력하세요.
# .env.local 파일은 절대 Git에 커밋하지 마세요!

# ==========================================
# MongoDB 데이터베이스 연결
# ==========================================
# 
# MongoDB Atlas 연결 문자열 형식:
# mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
#
# 설정 방법:
# 1. https://mongodb.com/cloud/atlas 에서 무료 계정 생성
# 2. Free Tier (M0) 클러스터 생성
# 3. Database Access에서 사용자 생성 (비밀번호 기록)
# 4. Network Access에서 0.0.0.0/0 추가 (개발용)
# 5. Connect > Connect your application > 연결 문자열 복사
# 6. <password>와 <database>를 실제 값으로 교체
#
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/loneat?retryWrites=true&w=majority

# ==========================================
# JWT 인증 토큰 설정
# ==========================================
#
# 강력한 랜덤 문자열 생성 방법:
# - Node.js: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# - OpenSSL: openssl rand -hex 64
#
# 주의: 프로덕션에서는 반드시 새로운 비밀키를 생성하세요!
#
JWT_SECRET=your_very_long_and_secure_random_string_here_at_least_64_characters

# ==========================================
# Naver Maps API (지도 기능 - 선택사항)
# ==========================================
#
# 설정 방법:
# 1. https://console.ncloud.com 에서 계정 생성
# 2. 콘솔 > AI·Application Service > AI·NAVER API > Application 등록
# 3. Web Dynamic Map 서비스 선택
# 4. Application 생성 후 Client ID 복사
#
NEXT_PUBLIC_NAVER_MAP_CLIENT_ID=your_naver_map_client_id

# ==========================================
# 앱 설정
# ==========================================
NEXT_PUBLIC_SITE_URL=https://loneat.kr
